[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "lumpy-la"
version = "0.1.1"
description = "Very elegant. Very Lumpy."
requires-python = ">=3.9"
dependencies = ["numpy"]

[tool.setuptools]
packages = ["lumpy"]
